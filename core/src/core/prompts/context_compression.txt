Compress context while preserving all facts, file paths, commands,
error messages, and structured data needed for task execution.

Reason step by step:
1. Identify critical facts: file paths, commands, error messages, configuration values
2. Distinguish redundant content from unique, actionable information
3. Preserve ALL actionable information — paths, commands, errors, decisions
4. Compress verbose explanations into concise summaries
5. Maintain structural relationships between pieces of information

Rules:
- NEVER fabricate, infer, or add information not present in context_text.
- Preserve exact file paths, URLs, error messages, code snippets, and numbers.
- Remove only true redundancy (repeated phrasing, filler, verbose formatting).
- When max_length is provided, prioritize retaining actionable information
  (commands, paths, errors, decisions) over narrative.
- If context_text is already within max_length, return it unchanged.