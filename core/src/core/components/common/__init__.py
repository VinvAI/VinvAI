"""core / components / common — Shared infrastructure.

Submodules are imported directly (e.g. ``from core.components.common.exceptions
import VinvEngineError``); this package exposes no eager re-exports so that
importing one submodule does not pull in the others.
"""
