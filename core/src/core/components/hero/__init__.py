"""core / components / hero — shared data models.

Only ``models`` is vendored into core (the Architect/Executor/Auditor actor
pipeline is not). Import it directly: ``from core.components.hero.models import ...``.
"""
