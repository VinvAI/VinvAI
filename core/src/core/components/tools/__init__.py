"""core / components / tools — executor tool implementations.

Only the ``terminal`` and ``file`` tool categories are vendored into core.
Import them directly, e.g.::

    from core.components.tools.terminal.terminal_tools import send_terminal_command
    from core.components.tools.file.file_tools import save_file
"""
