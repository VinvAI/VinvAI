"""core / components / bootstrap — path helpers.

Only ``paths`` is vendored into core; the full ``startup`` bootstrap (env
validation, tool registration, browser/terminal init) stays in the backend.
Import submodules directly, e.g. ``from core.components.bootstrap.paths import ...``.
"""
