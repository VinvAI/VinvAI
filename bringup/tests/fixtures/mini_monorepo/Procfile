# Heroku-style process types — one declared runnable per line.
web: python -m uvicorn app.main:app --host 0.0.0.0 --port 8100
worker: celery -A app.tasks worker --loglevel=info
clock: celery -A app.tasks beat --loglevel=info
