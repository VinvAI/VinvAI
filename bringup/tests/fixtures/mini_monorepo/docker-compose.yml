services:
  app:
    build: .
    command: python -m uvicorn app.main:app --host 0.0.0.0 --port 8100
    ports:
      - "8100:8100"
    depends_on:
      - db
  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: fixture
    ports:
      - "5432:5432"
